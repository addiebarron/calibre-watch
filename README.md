1. Install `python3` and `pipenv`: ```brew install python3 && python3 -m pip install --user pipenv```
2. Copy .plist file to `/Library/LaunchAgents/`
3. `launchctl load /Library/LaunchAgents/com.addie.calibrewatch.plist`
